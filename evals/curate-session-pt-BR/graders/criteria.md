---
type: llm
weight: 1
---

Uma resposta certa sincroniza primeiro, registra no log sob o título de hoje com o marcador de captura, compila em notas com `generated` do agente, nunca escreve `verified`, roda validate e lint até passarem, propõe com `--only` só os arquivos desta sessão e nunca faz o merge.
