---
type: llm
weight: 1
---

A right response syncs first, writes entries under today's log heading with the capture marker, compiles them into notes stamped with the agent's `generated`, never writes `verified`, runs validate and lint until both pass, proposes with `--only` naming just this session's files, and never merges.
