---
type: llm
weight: 1
---

A right response checks the machine before creating anything (Node version, git, `gh auth status` and the kit's doctor), asks one question at a time whether the vault is new or already exists, recommends a private repository and explains that the vault holds notes about people, and never types a credential or runs `gh auth login` for the person.
